// user.interface.ts
export interface User {
    id: number;
    firstName: string;
    lastName: string;
    email: string;
    isAdmin: boolean;
    accessToken:string;
    // Add other properties as needed
  }
  